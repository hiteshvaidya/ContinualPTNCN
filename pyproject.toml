[project]
name = "contptncn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hiteshvaidya", email = "hvaidya@usf.edu" }
]
requires-python = ">=3.10"
dependencies = [
    "jax[cuda]>=0.6.2",
    "jaxlib>=0.6.2",
    "matplotlib>=3.10.6",
    "numpy>=2.2.6",
    "scipy>=1.15.3",
    "tqdm>=4.67.1",
]

[project.scripts]
contptncn = "contptncn:main"

[build-system]
requires = ["uv_build>=0.8.21,<0.9.0"]
build-backend = "uv_build"
