[project]
name = "continual-ptncn"
version = "0.1.0"
description = "Parallel Temporal Neural Coding Network with fast weights"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.0.0",
    "torchtext>=0.15.0",
    "pytest>=7.0.0",
    "datasets>=4.8.4",
]
